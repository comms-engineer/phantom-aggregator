services:
  phantom-aggregator:
    build: .
    container_name: phantom-aggregator
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    ports:
      - "8080:8080"
